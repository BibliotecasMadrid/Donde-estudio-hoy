dist/
__pycache__/
*.pyc

# Material de firma y paquetes de la app Android: NUNCA en el repo (es publico).
# Los archivos siguen en tu disco, solo dejan de versionarse.
BiblioMadrid - Google Play package/

# Los zips de PWABuilder llevan el keystore dentro. Fuera del repo, siempre.
*.zip
*.keystore
*.jks
*.aab
*.apk

# Claves de API. La de Places vive FUERA del repo, pero por si acaso.
.env
*.env

# Cache de descargas de Google Places (fotos originales sin recortar).
.fotos-cache/
